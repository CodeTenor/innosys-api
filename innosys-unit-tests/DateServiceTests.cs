using innosys_application.Services;
using System;
using Xunit;

namespace innosys_unit_tests
{
    public class DateServiceTests
    {
        //private readonly DateService _sut = new DateService();

        //[Fact]
        //public void CaluclateBusinessDueDate_ShouldReturnTrueWhen()
        //{
        //    // Arrange
        //    const string userId = "87b72706-02e9-4b59-b376-0fc0bbef42cc";

        //    AddCycleRequestModel requestModel = fixture.Build<AddCycleRequestModel>()
        //                                               .With(x => x.userId, new Guid(userId))
        //                                               .Create();

        //    identityContract.FindUserByIdAsync(userId).ReturnsNull();

        //    // Act
        //    var result = await Assert.ThrowsAsync<CatalystException>(() => _sut.AddCycleAsync(requestModel));

        //    // Assert
        //    Assert.Equal(ExceptionMessages.userNotFound, result.Message);

        //    cycleRepository.Received(0).Add(Arg.Any<Cycle>());
        //}
    }
}
